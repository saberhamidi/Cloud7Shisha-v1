import { FlavourPickerDirective } from './flavour-picker.directive';

describe('FlavourPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new FlavourPickerDirective();
    expect(directive).toBeTruthy();
  });
});
