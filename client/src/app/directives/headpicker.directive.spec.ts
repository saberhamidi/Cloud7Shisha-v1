import { HeadpickerDirective } from './headpicker.directive';

describe('HeadpickerDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadpickerDirective();
    expect(directive).toBeTruthy();
  });
});
